# Visible deltas — friend walkthrough + Levels lens

Before: flat basic booker, chip-heavy durations, no first-run guidance, homepage felt like a poster + three CTAs.

After (5 visible deltas):

1. **Friend walkthrough cards** float over real desk/home (5 cards, Skip always, persist `clearline.onboarded.v1`) — teach by doing toward a bookable link, then get out of the way.
2. **Booking surfaces are real cards** — host card, duration cards (30/60/20), slot cards with hover lift, hairline borders, layered shadow (Clearline flat materials; no glass/purple).
3. **Empty-state coach** on desk calendar: one sentence + Open public page / Try with sample.
4. **Levels homepage** — one personal job above the fold (“Get a bookable link in one sitting”), **live booking mini** (host + week + slots), one primary CTA, no fake ratings.
5. **Opinionated defaults unchanged & surfaced** — Maya Mon–Fri windows + Intro 30 already live at `/b/maya` so first success needs almost no setup.

Activation: open `/b/maya` → week → slot → confirm (or share host page).
